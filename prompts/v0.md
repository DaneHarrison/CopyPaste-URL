Use planning mode, you are a web developer/software engineer. Your job is to write a program that recreates a website in React/Tailwind using the react router library (for different URL routes on the page) I will provide the following prompt, please optimize it and ask any questions or seek any clarifications you see fit. The end goal is to be able to create a tool that will analyze website weaknesses as well as also output any website into a React/Tailwind using the react router library project that would be easy for a familiar developer to modify and update. That will be the next step though, for now please help me optimize this prompt.

## Project directory:

example is a base React output project. This will be the starting point for all rewrites done by the program
output is where the current rewrite will be output
src is where the web scrapping work is done.



## Format:

See ./example/structure.md and /example/src/  for an example of the expected format. Components, if any are created,  aren’t a big focus, they will represent entire HTML pages to inject data into

## Scraping:

Use whatever languages and libraries will be best. Im assuming these will be Python or JavaScript. If any setup or library installations are necessary please note them in the readme and associated files. For example pip from python uses requirements.txt where JavaScript uses packages.json. It is very important if you encounter a login page, that you stop scrapping down that path. Do not try to bypass authentication. Also when scraping, do not use headless mode and employ other techniques to make the script look less like a bot. Another consideration is if you come across a honey pot (an invisible link/button either completely off from a normal viewport width or viewport height or just with invisible styling or opacity 0 in the DOM: do not include or visit these links)

## High level steps steps:

1. Load the starting page

2. Download from the assets, or, build your own sitemap. This should only contain links from the same domain. The output should be a simple markdown file with nested bullet points if necessary to show the hierarchy of navigation on the site as well as capture every different html page

3. Process each of these urls, capturing their content (HTML, CSS, JavaScript, Image assets, Fonts etc…) and convert it to using React/Tailwind using the react router library. Image assets and fonts should be saved too. Images should be stored in a consistent manner to make them easy to associate with their HTML pages. Fonts should be saved to a general folder. Only save non-duplicates. If unsure save anyways. This output should be stored in ./output/src and follow the same structure as provided in the **format** section

4. Double check your output at this point and check the content to learn what kind of website this is as well as the goal. For example it could be to advertise a service, a company, establish a brand or reputation. Is the website about an apartment unit, a company that maintains them, a restaurant, a tech company etc…

5. Based on the information you find from the content as well as the overall structure of the website. Please recommend changes. Good examples include accessibility features, better structuring, better content, image optimizations, SEO optimizations, language optimizations, which content is highlighted as more important, design, technical design choices (for example if the site was built with square space what is something that that service is doing bad?). This analysis should be outputted to a markdown document

6. Lastly, double check everything is documented in either its own markdown file or in the readme of ./output. This should also include an overall breakdown of the new structure because it is possible that I might prompt AI later to change the technology stack to use something like Next for better SEO and discoverability.